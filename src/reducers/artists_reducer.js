export default function manageArtists(state = {artists: [], loading: false}, action) {
  switch (action.type) {

    case 'LOADING_ARTISTS':

      return state

    case 'ADD_ARTISTS':

      return state

    default:
      return state;

  }
};
