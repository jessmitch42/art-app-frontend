export default function manageArtworks(state = {artworks: [], loading: false}, action) {
  switch (action.type) {

    case 'LOADING_ARTWORKS':
      return state;

    case 'ADD_ARTWORKS':

      return state;

    default:
      return state;

  }
};
